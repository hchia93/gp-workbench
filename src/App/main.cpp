// Phase 0 placeholder. Replaced in Phase 1 by AppMainWindow + theme bootstrap.
#ifdef _WIN32
#include <windows.h>
int WINAPI WinMain(HINSTANCE, HINSTANCE, LPSTR, int) { return 0; }
#else
int main() { return 0; }
#endif
